<Window x:Class="EWP_WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EWP_WPF" d:DataContext="{d:DesignInstance Type=local:ViewModel}"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800" WindowStyle="None"
        MaxWidth="{StaticResource {x:Static SystemParameters.MaximizedPrimaryScreenWidthKey}}" 
        MaxHeight="{StaticResource {x:Static SystemParameters.MaximizedPrimaryScreenHeightKey}}"
        MinHeight="32"
        MinWidth="144"
        MouseMove="Window_MouseMove">
    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="0"></WindowChrome>
    </WindowChrome.WindowChrome>
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Styles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <Grid Background="LightGray">
        <Grid.RowDefinitions>
            <RowDefinition Height="32"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="23*"/>
            <ColumnDefinition Width="141*"/>
            <ColumnDefinition Width="48"/>
            <ColumnDefinition Width="48"/>
            <ColumnDefinition Width="48"/>
        </Grid.ColumnDefinitions>
        <Button Grid.Column="4" Click="Button_Click" Panel.ZIndex="1">
            Close
        </Button>
        <Button Grid.Column="3" Click="Button_Click_1" Panel.ZIndex="1">Max
        </Button>
        <Button Grid.Column="2" Click="Button_Click_2" Panel.ZIndex="1">Min
        </Button>
        <TabControl Grid.RowSpan="2" Grid.ColumnSpan="5" >
            <TabItem Header="{Binding HomeTab}" Style="{StaticResource HomeTabStyle}" VerticalAlignment="Center">
                <local:UserControl1/>
            </TabItem>
        </TabControl>
        <Button Grid.Column="1" Panel.ZIndex="1" Width="30" HorizontalAlignment="Left" Content="+" />
    </Grid>
</Window>
